services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: nakama_estimate
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - nakama_pgdata:/var/lib/postgresql/data

volumes:
  nakama_pgdata:
